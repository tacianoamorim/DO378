package org.acme.conference.jwt;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeJwtResourceIT extends JwtResourceTest {

    // Execute the same tests but in native mode.
}